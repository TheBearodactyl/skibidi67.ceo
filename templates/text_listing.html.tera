{% extends "base" %}

{% block title %}Text — {{ site_host }}{% endblock %}
{% block og_title %}Text — {{ site_host }}{% endblock %}

{% block content %}
<h2>Text</h2>

{% if items | length == 0 %}
  <p>No text files yet.{% if user %} <a href="/ui/upload">Be the first to upload!</a>{% endif %}</p>
{% else %}
  <ul>
    {% for v in items %}
    <li>
      <a href="/ui/text/{{ v.id }}">{{ v.title }}</a>
      {% if v.nsfw %}[NSFW]{% endif %}
      {% if v.unlisted %}[unlisted]{% endif %}
      {% if v.references_id %}[dedup]{% endif %}
      — {{ v.uploaded_by_name }}, {{ v.uploaded_at_display }}, {{ v.size_human }}
    </li>
    {% endfor %}
  </ul>
{% endif %}

<p><a href="/ui">← Back</a></p>
{% endblock %}
